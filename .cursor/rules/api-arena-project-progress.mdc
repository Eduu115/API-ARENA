---
description: Estado actual del proyecto API Arena - servicios implementados, lo que falta, y arquitectura.
alwaysApply: true
---

# Progreso del Proyecto API Arena

## Microservicios implementados y funcionando en Docker

| Servicio | Puerto | Estado | Notas |
|----------|--------|--------|-------|
| **auth-service** | 8081 | ✅ Completo | JWT, registro, login, refresh, **Cloudflare Turnstile** en login/registro (`TURNSTILE_SECRET_KEY` / `VITE_TURNSTILE_SITE_KEY`; omit secret en dev = sin widget), **verificación de email** (Resend + token 48h; **solo** email de verificación en registro); **recordatorio programado cada ~2 meses** (`PasswordReminderScheduler`, `password_changed_at` / `last_password_reminder_sent_at`, email vía Resend con enlace a `/forgot-password`; cron UTC configurable); **tras verificar**: email bienvenida beta/legacy + “First steps” + creación notificación in-app vía notification-service; **mirror** a email solo notificaciones **IMPORTANT** (`POST /internal/users/{id}/notification-email`), flag `beta_legacy`, perfiles públicos, **badges coleccionables** (`profile_badge_definitions` / `user_profile_badges`, `GET/PUT /api/auth/me/badges`, selección hasta 5 en perfil; rank TOP 25 siempre visible aparte), **`GET /api/auth/leaderboard/elo`** / **`/level`**, **grupos de profesor con co-profesor opcional** (`co_teacher_id`), listado `GET /api/groups` incluye grupos donde el usuario es titular o co-titular; **`PUT /api/groups/{id}/co-teacher`** `{ coTeacherId }` (solo titular; `null` quita co-titular); **`GET /api/groups/search-teachers`**; amistades, `/internal/users/{id}/reward`, logros, tiempo desarrollo/navegación; **opt-in alumnos** `new_challenge_email_alerts` + `PUT /api/auth/me` con `newChallengeEmailAlerts`; **`POST /internal/challenges/new-published`** (token `X-Internal-Token` / `services.internal-token`) envía email vía Resend a estudiantes verificados suscritos (excluye autor); **`GET /internal/teachers/{tid}/students/{uid}/in-group`** (mismo token) para submission-service: alumno en algún grupo donde el profesor es titular **o** co-titular; **recuperación de contraseña** (`POST /api/auth/forgot-password` genérico + `POST /api/auth/reset-password`, token 1h en `users.password_reset_token`, email vía Resend, revoca sesiones); **GDPR**: `GET /api/auth/me/export` (portabilidad JSON) y `DELETE /api/auth/me` (derecho de supresión: revoca tokens, borra usuario con cascada y llama a submission-service para purgar entregas/ZIPs); `List-Unsubscribe` en email de nuevos challenges; **racha semanal ISO UTC**: tablas `user_streak_state` / `user_weekly_progress` / `user_weekly_streak_challenges`, vía A ≥120 XP o vía B 3 challenges distintos con `pipeline_total_score` ≥650 (anti-repeat 3 semanas), hook en `applyReward`, `GET /api/auth/me/streak`, expuesto en perfil público; logros `WEEKLY_STREAK_4` / `WEEKLY_STREAK_12`; **i18n backend v1**: `users.preferred_locale` (`en|es`, default `en`), registro/`PUT /api/auth/me`, emails Resend EN/ES con URLs `/{locale}/…`, errores auth con `ApiException` + `{ code, message }` |
| **challenge-service** | 8082 | ✅ Completo | CRUD challenges, categorías, featured, legacy/community, slug, `xpReward`, `timeLimitMinutes`; **vista pública** `GET /api/challenges/{id}/preview` (sin specs técnicas); **specs completas** `GET /api/challenges/{id}/specs` (JWT) para flujo submit; `GET /api/challenges` por ID sigue devolviendo DTO completo para servicios internos; **nuevos challenges creados como borrador** (`isActive=false`); al pasar a activo (`update`) notifica async a auth para emails de “nuevo challenge”; `SERVICES_AUTH_URL` + `SERVICES_INTERNAL_TOKEN` en Docker |
| **submission-service** | 8083 | ✅ Completo | Upload ZIP, pipeline async (sandbox build → testing HTTP → COMPLETED), scores, logs, métricas, WebSocket, XP+ELO (HTTP a auth/challenge), **Kafka producer** `apiarena.submissions.completed`, **anti-farm**, **replay** en Postgres + **espejo MongoDB** (`replay_archives`, opt-in `REPLAY_MONGODB_ENABLED`), **InfluxDB 2** medición `submission_pipeline` (opt-in `INFLUX_ENABLED`), `GET /api/submissions/{id}/replay`, **endpoints teacher-scoped** para listar/contar entregas por alumno en challenges propios, **`GET /api/submissions/teacher/challenges/{challengeId}/submissions`** (todas las entregas del challenge si el profesor es autor) y descarga ZIP por entrega, retención programada; listados incluyen `zipDownloadExpiresAt` (informativo, `submission.zip-availability-days`) y descarga expone cabecera `X-Zip-Download-Expires-At`; resumen con `submitterUsername` cuando aplica; **ajuste de nota por profesor** (dueño del challenge): `POST /api/submissions/{id}/teacher/penalty` (presets + OTHER), `POST /api/submissions/{id}/teacher/manual-scores` (solo si el alumno está en un grupo del profesor, vía auth interno), **`PUT /api/submissions/{id}/teacher/submission-review`** (nota personal, comentarios por zona, feedback estructurado tipo AI, líneas de bonus positivas acumuladas ≤1000, opción `notifyStudent` → notification-service); **`GET /api/submissions/teacher/corrections-queue`** (JWT reenviado a challenge `/mine`; listados teacher incluyen `teacherCorrectionComplete`); columnas `teacher_penalties`, `teacher_personal_note`, `teacher_zone_notes`, `teacher_structured_feedback`, `teacher_score_bonuses`, `teacher_manual_grading`; DTO con flags `teacherCanApplyPenalty` / `teacherCanManualGrade` / `teacherCanEditSubmissionReview`; **ELO** penaliza repetir por debajo de tu mejor nota en el mismo challenge, evita cambio 0 cuando sigues por debajo de lo esperado, y suma castigo extra si la nota es muy baja (`docs/xp-elo-system.md`); **GDPR erasure** `DELETE /internal/users/{userId}/data` (token `X-Internal-Token`) purga entregas del usuario: borra ZIP en disco, replays Mongo, filas `sandbox_executions` huérfanas y entregas (cascada `replay_events`/`test_results`); `deleteSubmission` también borra ZIP y `sandbox_executions`; **retención de ZIPs** `ZipRetentionService` (`@Scheduled`, `submission.zip-availability-days` 90d) borra el ZIP en disco y pone `zip_file_path=NULL` pasada la ventana (minimización GDPR); **`pipeline_total_score`** congelado al completar pipeline (antes de penalizaciones docente) para racha semanal |
| **sandbox-service** | 8084 | ✅ Operativo | Runner dual por flag `sandbox.runner.mode=process|dind`. En `dind`: **contenedor por submission** en red **interna** `apiarena-candidate` (sin salida a Internet ni a DB), cleanup contenedor+imagen, readiness HTTP, token interno, hardening (`--init`, `--ulimit nofile`, caps, read-only, tmpfs `/tmp`) |
| **testing-service** | 8085 | ✅ Operativo | **Tests HTTP reales** (`RestTemplate`) contra la URL del candidato: funcionales (según `testSuite` del challenge), rendimiento y diseño; puntuación agregada. No sustituye tests unitarios dentro del ZIP del alumno — es caja negra contra la API levantada |
| **leaderboard-service** | 8087 | ✅ Completo | Rankings globales y por challenge; **`GET /api/leaderboard/global/user/{userId}`** (posición global por score acumulado); **consumer Kafka** actualiza entradas al completar submission |
| **ai-review-service** | 8086 | ✅ Operativo (v2 ready) | Endpoint interno `POST /internal/ai-review/analyze`, provider configurable (`heuristic|gemini`), fallback automático a heurístico, `aiScore` 0-200 + feedback estructurado; listo para API key por entorno |
| **multiplayer-service** | 8088 | ❌ No creado | |
| **metrics-service** | 8089 | ✅ Operativo | Agregados (`/api/metrics/overview`, `/api/metrics/submissions/daily`), feedback docs, **eventos de producto** (`POST /api/metrics/events`, **protegido con `X-Internal-Token`**: ingesta solo servidor-a-servidor, 401 sin token), **BI summary/export** (`/api/metrics/bi/summary`, `/api/metrics/bi/export?format=json|csv`), **export CSV programado (opt-in)** `METRICS_BI_EXPORT_ENABLED` + volumen `/app/bi-exports`, Prometheus; lectura de negocio restringida a ADMIN |
| **notification-service** | 8090 | ✅ Completo | In-app + **WebSocket** `/ws/notifications` (JWT query); Kafka incl. **challengeTitle**; REST JWT; **importancia** `INFO|REMINDER|ALERTS|IMPORTANT`; **solo** `IMPORTANT` → email vía auth (mismo título/cuerpo); bienvenida in-app tras verificar email (auth → `POST /internal/notifications/welcome`); **achievement unlocked** auth → `POST /internal/notifications/achievement-unlocked` (tipo `ACHIEVEMENT_UNLOCKED`, push WS + toast frontend); revisión docente submission-service → `POST /internal/notifications/teacher-submission-review` (tipo `TEACHER_SUBMISSION_REVIEW`, INFO + push WS); query `minImportance`; frontend: `/notifications` + push toast global en topbar | |

## Infraestructura Docker

- **PostgreSQL**: ✅ `init-db.sql` (seed, friendships, etc.)
- **Redis**: ✅ (auth rate-limit + submission cache)
- **MongoDB**: ✅ **integrado** en submission-service (archivo de replay `replay_archives`; lectura si Postgres vacío tras retención)
- **InfluxDB**: ✅ **integrado** en submission-service (serie `submission_pipeline`: duración, score, estado)
- **Kafka + Zookeeper**: ✅ **evento real** submission completada → leaderboard + **notifications** (topic `apiarena.submissions.completed`)
- **Prometheus + Grafana + Alertmanager**: ✅ `9090` / `3001` / `9093`; reglas `docker/prometheus/alerts.yml` (HTTP 5xx, heap JVM); datasource **InfluxDB** en Grafana (UID `influx-apiarena`; token alineado con `.env`); dashboards Operational Overview + Submission Pipeline Health
- **Frontend (Nginx)**: ✅ puerto 3000; rutas **`/en/`** y **`/es/`** (redirect `/` → locale guardado o `Accept-Language`); SEO canonical + hreflang; nav/chrome labels siguen en inglés; **`LocaleLink`** + **`useNavigateLocalized`** en toda la app; sync `preferred_locale` ↔ URL vía `LocalePreferenceSync`

## Sistema de XP y ELO

Ver `docs/xp-elo-system.md`. (Sin cambio de reglas de negocio recientes.)

## Frontend — Páginas implementadas

| Ruta | Componente | Estado |
|------|------------|--------|
| `/` | Landing | ✅ Sin tour (tours solo tras login + email verificado, 1ª visita por cuenta); footer con enlaces legales + **Cookie preferences** |
| (global) | CookieConsent | ✅ Banner de consentimiento (necesarias siempre + **preferences** opt-in + analytics reservado); gating de persistencia de tema/tutoriales según consentimiento; reabrible desde footer y página `/cookies` |
| `/login` | Login | ✅ |
| `/register` | Register | ✅ |
| `/verify-email` | VerifyEmail | ✅ Enlace con `?token=`, reenvío |
| `/forgot-password` | ForgotPassword | ✅ Solicitud de reset (respuesta genérica) |
| `/reset-password` | ResetPassword | ✅ Nueva contraseña con `?token=` |
| `/challenges` | Challenges | ✅ Sidebar **Email alerts** (solo STUDENT); tour catálogo 1ª vez |
| `/challenges/:id` | ChallengeDetail | ✅ Resumen público (`/preview`); specs técnicas solo en submit; tour ficha 1ª vez |
| `/challenges/:id/submit` | ChallengeSubmit | ✅ Timer (`deadlineAt`), límites, sesión; tour submit tras cerrar modal 1er envío |
| `/dashboard` | Dashboard | ✅ Tour 1ª vez; **widget racha semanal** (sidebar + panel) vía `GET /api/auth/me/streak` |
| `/submissions` | MySubmissions | ✅ (título del challenge vía `challengeTitle` en `GET /api/submissions/my`) |
| `/submissions/:id` | SubmissionDetail | ✅ |
| `/submissions/:id/results` | SubmissionResults | ✅ |
| `/leaderboard` | Leaderboard | ✅ Modos **ELO** (default), **Level** (XP) y **Challenge** (selector obligatorio + ranking por score del challenge-service); podium y tabla adaptados por modo |
| `/perfil` | Profile | ✅ Hero identidad, KPIs, **badges coleccionables** (unlock + elegir hasta 5; rank TOP 25 obligatorio), achievements preview, racha, bio/GitHub, Privacy & data |
| `/perfil/achievements` | ProfileAchievements | ✅ Catálogo completo con filtros All / Unlocked / Locked |
| `/friends` | Friends | ✅ |
| `/notifications` | Notifications | ✅ Campana en topbar + WebSocket + lista |
| `/docs` + `/docs/:docId` | DocsHub | ✅ Mini-routing, feedback; orden de guías: **Getting Started** → **First Steps** → **Preconfigure Project** → resto; CTA “Continue learning” al pie; `/docs` redirige al primer documento; tours `DOCS_PATHS` apuntan a Getting Started; **switch EN | ES** en cabecera (contenido + chrome del hub traducido; persistencia en `localStorage` con consentimiento de cookies *preferences*) |
| `/replay` | Replay | ✅ Timeline real; filtros etapa + HTTP/tests; salto a fallo; flechas; metadata JSON por evento; índice `testIndex` |
| `/multiplayer` | MultiplayerHub | ⚠️ Parcial / mock |
| `/teacher/*` | Teacher area | ✅ Groups: botón `x entregas` por alumno + vista de entregas en challenges del profesor con acciones `View`, `Results`, `Download ZIP` (cuenta atrás de retención ZIP + deshabilitar si caducó); **dashboard** `/teacher`: “My challenges & submissions” (refetch al expandir; filas **TODO / CORRECTED** según `teacherCorrectionComplete`); **Recent corrections** datos reales vía `GET /api/submissions/teacher/corrections-queue`; KPI pending = entregas sin corrección docente; **Corrections** `/teacher/corrections` lista la cola con filtros; grupos **Shared** + co-profesor en `/teacher/groups`; **Submission detail**: penalizaciones, manual grading, **Submission review & score bonuses**; alumno ve **Teacher review** cuando hay contenido |

## Flujo end-to-end verificado

1. Login → JWT  
2. Challenges → Detail → Submit (timer + límites + ZIP)  
3. POST submission → **sandbox** (Maven + JAR) → **testing** (HTTP real) → COMPLETED  
4. **Kafka** → leaderboard actualizado; **HTTP** → auth reward XP/ELO  
5. Results / submissions / leaderboard coherentes  

### Límites conocidos (sandbox / testing)

- **Aislamiento DinD**: runtime del candidato en red **internal** (`apiarena-candidate`); build Maven sigue usando red de BuildKit. Pendiente: rootless DinD / políticas extra en prod.  
- **Testing**: acoplado a contratos HTTP del challenge (`testSuite`, endpoints por defecto si faltan datos).  

## Credenciales de prueba (seed)

| Usuario | Email | Password | Rol |
|---------|-------|----------|-----|
| arclight | arclight@apiarena.dev | Arena2025! | STUDENT |
| byterunner | byterunner@apiarena.dev | Arena2025! | STUDENT |
| profoak | profoak@apiarena.dev | Arena2025! | TEACHER |
| sysop | sysop@apiarena.dev | Arena2025! | ADMIN |

## Hecho recientemente (resumen)

- Kafka: producer en submission-service, consumer en leaderboard-service, `docker-compose` con `depends_on` kafka.  
- Anti-farm: cooldown + 3 intentos/día UTC; endpoint `attempt-status`; staff sin límite.  
- Challenge submit: sesión localStorage con deadline en tiempo real; banner global; UI de límites destacada; fix doble file picker.  
- Email verification: Resend (opcional vía `RESEND_API_KEY`), tokens en `users`, frontend `/verify-email`, login bloqueado hasta verificar.  
- Achievements v1: auth-service (definiciones + concesión por stats/cohort), perfil con lista y tiers.  
- Achievements v2: catálogo ampliado (~24 defs: clears, XP/ELO, perfil, tiempo coding/browsing, streaks); preview en `/perfil` + página `/perfil/achievements` con filtros.
- **Rate limiting HTTP (Redis)**: auth-service (login/register/refresh/forgot/reset/verify + perfiles públicos por IP; friend-request + usage heartbeat por usuario); submission-service burst en `POST /api/submissions` (IP + usuario, staff bypass); metrics docs-feedback 10/min por IP in-process.
- Hardening P1: runner DinD con fallback por flag, replay estructurado persistido y stack metrics/prometheus/grafana.
- Automatización alta prioridad: `scripts/e2e-smoke.sh`, `scripts/load-submissions.sh`, workflow CI `.github/workflows/e2e-smoke.yml`.
- AI review v2 ready: rúbrica final **800 técnico + 200 IA** en `submission-service`, proveedor configurable en `ai-review-service` y wiring de variables para API key en `.env` / Compose.
- Docs learning hub: mini-routing `/docs/:docId`, contenido largo por documento, guía de preconfiguración, bloque visual tipo markdown y captura de feedback para decisiones de contenido.
- Tours UI (overlay, highlight `data-tutorial`, skip, Open Docs): dashboard, catálogo, ficha challenge, submit (solo usuario autenticado y email verificado; `localStorage` por `tourKey` + user id); submit tras modal primer envío.
- Onboarding primer challenge: modal de confirmación en submit para primeros envíos con acceso directo a la guía de preconfiguración.
- Métricas de negocio no públicas: endpoints de consulta en metrics-service protegidos para rol ADMIN; usuarios estándar solo pueden emitir feedback.
- BI-ready baseline: tabla `product_events` + endpoint de ingestión y export CSV/JSON por ventana temporal para conectar con BI apps/GA pipelines.
- Backlog priorizado (local): `docs/BACKLOG-NEXT-ITERATION.md`; Grafana pipeline dashboard; export BI programado; DinD `--init` + ulimit.
- Operativa: **Alertmanager** + reglas Prometheus; red candidato **internal**; **Mongo replay** + **Influx pipeline** + Grafana Influx datasource; replay UI mejorada.
- Teacher groups v2: en `/teacher/groups` cada miembro muestra `x entregas` (solo challenges del profesor), panel de historial por alumno y descarga del ZIP entregado.
- Teacher submission review: `PUT /api/submissions/{id}/teacher/submission-review` + UI en `/submissions/:id`; notificación in-app `TEACHER_SUBMISSION_REVIEW` (notification-service).
- Teacher corrections queue: `GET /api/submissions/teacher/corrections-queue` + dashboard “Recent corrections” y `/teacher/corrections` sin mock; `teacherCorrectionComplete` en listados teacher.
- Public profile **last seen**: columna `users.last_seen_at` (login + heartbeat navegación throttled 2 min); `lastSeenAt` en `GET /api/auth/users/{id}/profile`; modal perfil público “Online now” / “Last online …”.
- Cooldown UX: en ficha del challenge no se entra a `/submit` con cuenta atrás visible; modal con tiempo restante; pantalla submit bloqueada con el mismo modal y sin arrancar timer/sesión hasta que la política permita.
- Challenge público vs specs: ficha `/challenges/:id` solo preview (nombre, descripción, stats, teaser); endpoints/tests/hints solo vía `GET .../specs` en pantalla submit autenticada.

## Lo que falta (priorizado) y orden de magnitud

Estimaciones **muy orientativas** (1 dev familiarizado con el repo; dependen de alcance y calidad “production-ready”).

| Ítem | Notas | Duración estimada |
|------|--------|-------------------|
| Ampliar Kafka (más topics, DLQ, consumidores extra) | Opcional | **2–5 días** |
| **Sandbox “duro”** (hardening final de aislamiento/red) | DinD base ya listo; faltan políticas avanzadas | **2–5 días** |
| **metrics-service** + dashboards básicos | ✅ Entregado en v1, faltan dashboards de negocio avanzados | **2–4 días** |
| **Notificaciones** | Push nativo / email / WebSocket tiempo real (v1 = in-app + Kafka ya hecho) | **1–2 semanas** |
| **Replay funcional** | ✅ Timeline real entregado; faltan features avanzadas (diff payload/seek por testcase) | **3–7 días** |
| **Grafana + InfluxDB** | ✅ Escritura pipeline + datasource Grafana; paneles avanzados opcionales | **2–4 días** |
| **MongoDB** logs / replay storage | ✅ Archivo `replay_archives` + retención alineada; ampliar índices/TTL según carga | **0.5–1 semana** |
| **ai-review-service** (LLM) | ✅ Base v2 lista (falta calibración fina de prompts/rúbrica por challenge y control de costes) | **2–6 días** |
| **multiplayer-service** | BONUS, WebSocket + salas | **3–6 semanas** |
| **Achievements (ampliar)** | ✅ v2 catálogo + preview perfil + notificación in-app/WS al desbloquear; faltan iconos asset | **1–2 días** |

**Núcleo “MVP producto estable”** (Kafka ya hecho, límites hechos, listado de submissions con título de challenge, verificación de email con Resend, métricas mínimas): del orden de **~3–6 semanas** de trabajo enfocado.  
**Con BONUS** (AI review + multiplayer + replay fuerte): **varios meses** adicionales en paralelo o secuencial.

## Archivos clave

- `docker-compose.yml`
- `docker/postgres/init-db.sql`
- `docs/xp-elo-system.md`
- `docs/TECHNICAL-OVERVIEW.md` — documento técnico integral (stack, arquitectura, hecho/por hacer)
- `docs/COMMIT-CONF-SPAIN-PROJECT-BRIEF.md` — briefing para propuestas (Commit Conf, etc.)
- `docs/GDPR-RAT.md` — Registro de Actividades de Tratamiento (art. 30 RGPD) + lista de encargados
- `frontend/src/lib/*` — API clients
- `frontend/src/routes/RoutesConfig.jsx`
- `frontend/src/pages/challenges/challenges.css` — tokens de marca
